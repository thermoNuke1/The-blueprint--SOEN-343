{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "test": "cross-env  NODE_ENV=test node --test",
    "setdep": "npm install axios && npm install express && npm install --save-dev nodemon && npm install cors && npm install --save-dev supertest && npm install dotenv && npm install mongodb && npm install mongoose && npm install bcrypt && npm install --save-dev cross-env && npm install express-async-errors --save-dev && npm install jsonwebtoken && npm install stripe"
  },
  "author": "Carlo Aldabi",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "bcrypt": "^5.1.1",
    "bootstrap": "^5.3.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.10.0",
    "mongoose": "^8.8.0",
    "mysql": "^2.18.1",
    "mysql2": "^3.11.3",
    "stripe": "^17.3.1",
    "react-bootstrap": "^2.10.5"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "express-async-errors": "^3.1.1",
    "nodemon": "^3.1.7",
    "supertest": "^7.0.0"
  }
}
